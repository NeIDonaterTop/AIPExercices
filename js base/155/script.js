// Задание 1
function printSum(param1, param2, param3) {
  console.log(param1 + param2 + param3);
}

// Даны переменные
let param1 = 1;
let param2 = 2;
let param3 = 3;

// Вызов функции
printSum(param1, param2, param3); 
